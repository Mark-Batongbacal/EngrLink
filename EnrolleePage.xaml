<Page
    x:Class="EngrLink.EnrolleePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EngrLink"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Button
        x:Name="BackButton"
        Content="←"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Width="40"
        Height="40"
        Click="BackButton_Click" />
        <!-- StackPanel will organize the elements vertically -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="15">
            <!-- Name Field -->
            <TextBox x:Name="NameTextBox" PlaceholderText="Enter Name" TextChanged="Input_TextChanged" Width="300" Height="40" Padding="10"/>

            
            <TextBox x:Name="AddressTextBox" PlaceholderText="Enter Address" TextChanged="Input_TextChanged" Width="300" Height="40" Padding="10"/>

            <!-- Contact Field -->
            <TextBox x:Name="ContactTextBox" PlaceholderText="Enter Contact" TextChanged="Input_TextChanged" Width="300" Height="40" Padding="10"/>

            <!-- Program Field -->
            <ComboBox x:Name="ProgramComboBox" Width="300" Height="50" Padding="10" Header="Enter Program" SelectionChanged="Program_SelectionChanged" >
                <ComboBoxItem Content="ARCHI" />
                <ComboBoxItem Content="CE" />
                <ComboBoxItem Content="CPE" />
                <ComboBoxItem Content="ECE" />
            </ComboBox>

            <!-- Year Field -->
            <ComboBox x:Name="YearLevelComboBox" Width="300" Height="50" Padding="10" Header="Enter Year Level" SelectionChanged="YearLevel_SelectionChanged">
                <ComboBoxItem Content="1st Year" />
                <ComboBoxItem Content="2nd Year" />
                <ComboBoxItem Content="3rd Year" />
                <ComboBoxItem Content="4th Year" />
                <ComboBoxItem Content="5th Year" />
            </ComboBox>

            <!-- Birthday Picker -->
            <DatePicker x:Name="BirthdayDatePicker" Header="Enter Birthday" DateChanged="Input_DateChanged" Width="300" Height="50"/>

            <!-- Submit Button -->
            <Button Name="SubmitButton" Content="Submit" Width="100" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Click="SubmitButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
