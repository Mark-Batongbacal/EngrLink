<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EngrLink.Main_Window.Students.SubPages.PersonalInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EngrLink.Main_Window.Students.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:EngrLink.Models"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollView>
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0" Width="400">
                <TextBlock Text="Personal Information" FontSize="40" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>

                <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20" Background="White">
                    <StackPanel Spacing="10">
                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Student ID:" />
                        <TextBlock Text="{Binding PersonalInfo.Id}" FontSize="18" FontWeight="SemiBold"/>

                        <Line Stroke="LightGray" StrokeThickness="1" Margin="0,5"/>

                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Name:" />
                        <TextBlock Text="{Binding PersonalInfo.Name}" FontSize="18" FontWeight="SemiBold"/>

                        <Line Stroke="LightGray" StrokeThickness="1" Margin="0,5"/>

                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Program:" />
                        <TextBlock Text="{Binding PersonalInfo.Program}" FontSize="18" FontWeight="SemiBold"/>

                        <Line Stroke="LightGray" StrokeThickness="1" Margin="0,5"/>

                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Year Level:" />
                        <TextBlock Text="{Binding PersonalInfo.Year}" FontSize="18" FontWeight="SemiBold"/>

                        <Line Stroke="LightGray" StrokeThickness="1" Margin="0,5"/>

                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Address:" />
                        <TextBlock Text="{Binding PersonalInfo.Address}" TextWrapping="Wrap" FontSize="18" FontWeight="SemiBold"/>

                        <Line Stroke="LightGray" StrokeThickness="1" Margin="0,5"/>

                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Contact No.:" />
                        <TextBlock Text="{Binding PersonalInfo.Contact}" FontSize="18" FontWeight="SemiBold"/>

                        <Line Stroke="LightGray" StrokeThickness="1" Margin="0,5"/>

                        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Birthdate:" />
                        <TextBlock Text="{Binding PersonalInfo.Birthday}" FontSize="18" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollView>
</Page>