<Page
    x:Class="EngrLink.Main_Window.Students.SubPages.Dashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EngrLink.Main_Window.Students.SubPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:EngrLink.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="Light">

    <Grid Padding="20">
        <StackPanel Spacing="20">
            <TextBlock Text="Student Dashboard" FontSize="28" FontWeight="Bold"/>

            <TextBlock Text="Announcements" FontSize="22" FontWeight="SemiBold" Margin="0,20,0,10"/>
            <ListView x:Name="AnnouncementsListView" ItemsSource="{x:Bind StudentAnnouncements, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Announcement">
                        <StackPanel Margin="0,5,0,5" Padding="10" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="5">
                            <TextBlock Text="{x:Bind Announcements}"
                                       FontSize="16"
                                       TextWrapping="WrapWholeWords"
                                       FontWeight="Medium"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                
            </ListView>

        </StackPanel>
    </Grid>
</Page>