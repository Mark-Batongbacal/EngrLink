<Page
    x:Class="EngrLink.Main_Window.Department_Chairman.DepartmentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EngrLink"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    RequestedTheme="Light">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- For the Banner -->
            <RowDefinition Height="*"/>
            <!-- For the Buttons -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <!-- Left panel fixed width -->
            <ColumnDefinition Width="*" />
            <!-- Right panel takes remaining space -->
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <StackPanel Grid.Column="0" Background="#ffc797" VerticalAlignment="Stretch" Spacing="1" Grid.Row="1">
            <Button x:Name="BackButton" Content="←" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Width="60" Height="40" Click="BackButton_Click" Grid.Row="1" />
            <AppBarSeparator/>
            <Button x:Name="Dashboard" Content="Dashboard" Width="200" Height="40" HorizontalContentAlignment="Left" Click="Dashboard_Click" />
            <Button x:Name="Schedules" Content="Schedules" Width="200" Height="40" HorizontalContentAlignment="Left" Click="Schedules_Click" />
            <Button x:Name="ListStudents" Content="List of Students" Width="200" Height="40" HorizontalContentAlignment="Left" Click="ListStudents_Click" />
            <Button x:Name="ListFaculty" Content="List of Faculty" Width="200" Height="40" HorizontalContentAlignment="Left" Click="ListFaculty_Click" />
            <Button x:Name="Announcements" Content="Announcements" Width="200" Height="40" HorizontalContentAlignment="Left" Click="Announcements_Click" />
            <Button x:Name="Enrollees" Content="Enrollees" Width="200" Height="40" HorizontalContentAlignment="Left" Click="Enrollees_Click" />
        </StackPanel>

        <!-- Content Frame -->
        <Frame x:Name="DepartmentChairFrame" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</Page>
