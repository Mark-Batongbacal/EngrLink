<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EngrLink.LoginStudent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EngrLink"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Button
    x:Name="BackButton"
    Content="←"
    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    Width="40"
    Height="40"
    Click="BackButton_Click" />
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="15">
            <TextBox x:Name="StudentID" PlaceholderText="Enter ID" TextChanged="Input_TextChanged" Width="300" Height="40" Padding="10"/>

            <PasswordBox x:Name="Password" PlaceholderText="Enter Password" PasswordChanged="Input_TextChanged" PasswordRevealMode="Peek" Width="300" Height="40" Padding="10"/>

            <TextBlock x:Name="PasswordInstruction" Text="New Users will have the same Password as their Student ID" Width="375" Height="40" Padding="10"/>

            <Button Name="ChangePasswordButton" Content="Change Password" Width="150" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Click="ChangePasswordButton_Click"/>
            
            <Button Name="SubmitButton" Content="Submit" Width="100" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Click="SubmitButton_Click"/>

            <PasswordBox x:Name="NewPassword" PlaceholderText="Enter New Password" Width="300" Height="40" Margin="0,10,0,0"/>
            <PasswordBox x:Name="ConfirmNewPassword" PlaceholderText="Confirm New Password" Width="300" Height="40"/>
            <Button Content="Change Password" Click="ChangePasswordButton_Click" Width="150" Margin="0,10,0,0"/>


        </StackPanel>
    </Grid>
</Page>
